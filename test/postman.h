#pragma once
#include "public.h"

class Solution {
public:
    int minStep(const vector<int>& city, const vector<int>& postman) {
        int result = 0;
        queue<int> tmp;
        set<int> temp;
        for (int i = 0; i < postman.size(); ++i) {
            tmp.push(postman[i]);

        }
        return result;
    }
};

#pragma once
#include "public.h"

class Solution {
public:
    string reverseParentheses(string s) {
        stack<char> temp;
        string result;
        for (int i = 0; i < s.size(); ++i) {
            
        }
    }
};
